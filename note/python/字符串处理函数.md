1. startwith
```python
str = 'hemingyang'
if (str.startwith('he'))
```

2. rstrip
移除字符串右边的指定字符，默认是\n,\t等,会连续删除，不修改原字符串
```python
str = 'hemingyang!!!'
str_rstriped = str.rstrip('!')
# output : hemingyang